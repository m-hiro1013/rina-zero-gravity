# 実行プラン: Phase 16 - ECC v2 実走検証＆専門スキル `sf-maker` の構築

デュアルコア・プロシード機能を持つ実行機へ：
以下のタスクを上から順に実行し、1つ完了するごとに**必ずこのファイルの `[ ]` を `[x]` に書き換えて上書き保存**してください。

---

## 🚀 実行ステップ

- [x] **1. グローバル環境の動作確認 (Sanity Check)**
  - `ls -la ~/.claude/skills` を実行し、44スキルが存在することを確認する。
  - `cat ~/.claude/skills/search-first/SKILL.md` 等を実行し、description が日本語の最強思想版（「既存の知見を探し出し…」等）になっているか確認する。

- [x] **2. 既存機能のテスト完勝 (Quality Gate)**
  - `cd /Users/matsumotohiroki/Developer/rina-zero-gravity/ZG_PROJECT/everything-claude-code` に移動する。
  - `node tests/run-all.js` を実行し、全テストにパスすることを確認する。
  - （失敗した場合は、原因ファースト原則に従い、原因を断定するまで推測でのコード修正を行わない）

- [x] **3. `sf-maker` (Search First Maker) の構築**
  - `mkdir -p ~/.claude/skills/sf-maker/` を作成する。
  - 以下の仕様を満たす `~/.claude/skills/sf-maker/SKILL.md` を作成する：
    - **name**: sf-maker
    - **description**: 新しいスキルを作成・提案する際、既存スキルとの重複や車輪の再発明を防ぐための「事前調査（search-first）」を強制する専門スキル。"create skill", "新しいスキル作って" 等で発動する。
    - **思想（プロンプト内容）**: 「スキル作成を依頼された場合、絶対にいきなり作らない。まずは既存の ~/.claude/skills 配下や MCP を検索し、似たような役割のスキルがないかを徹底的に調査する。既存のものがあればそちらを拡張・利用する方針をユーザーに提案する。」といった旨を記述。
  - `sf-maker` に自分自身（sf-maker）の概要を説明させる動作確認を行う（できれば `claude` との対話や `/status` 等での簡易確認）。

- [x] **4. 進捗の同期と記録保存**
  - `ZG_PROJECT/everything-claude-code` か、親プロジェクトである `rina-zero-gravity` 側の `prompt/WORKFLOW.yaml` に、Phase 16 が完了したことと `sf-maker` 作成の事実を記録・追記する。
  - プロジェクトの `prompt/ARCHITECTURE.yaml` に `sf-maker` の役割を追加する。

---
✅ すべてのタスクの `[x]` 化が完了したら、作業終了を宣言してください。
