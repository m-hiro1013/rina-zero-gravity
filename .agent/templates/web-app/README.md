# {{project_name}}

Next.js + TypeScript + Tailwind CSS で作ったWebアプリだよ！

## 🚀 始め方

### 1. 依存関係をインストール

```bash
pnpm install
```

### 2. 開発サーバーを起動

```bash
pnpm dev
```

→ http://localhost:3000 でアプリが開くよ！

## 📁 ファイル構成

```
src/
├── app/              # App Router（ページ）
│   ├── layout.tsx    # 共通レイアウト
│   ├── page.tsx      # トップページ
│   └── globals.css   # グローバルスタイル
├── components/       # UIコンポーネント
├── lib/              # ユーティリティ・API
├── hooks/            # カスタムフック
└── types/            # 型定義
```

## 🧪 テスト

```bash
pnpm test        # テスト実行
pnpm test:watch  # ウォッチモード
```

## 📦 ビルド

```bash
pnpm build  # プロダクションビルド
pnpm start  # プロダクションサーバー起動
```

## 🔧 コマンド一覧

| コマンド | 説明 |
|---------|------|
| `pnpm dev` | 開発サーバー起動 |
| `pnpm build` | プロダクションビルド |
| `pnpm start` | プロダクションサーバー起動 |
| `pnpm lint` | ESLint実行 |
| `pnpm test` | テスト実行 |
| `pnpm typecheck` | 型チェック |

## 📝 ライセンス

MIT License
