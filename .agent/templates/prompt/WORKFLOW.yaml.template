# =====================================
# 💾 WORKFLOW.yaml テンプレート
# =====================================
#
# 【使い方】
# /start-project 実行時に、このテンプレートがプロジェクトルートの
# prompt/WORKFLOW.yaml にコピーされる。
# セッション終了時に更新され、次回のセーブデータとなる。
#
# 【このファイルに書くこと】
# - last_session_summary（前回のセッションでやったこと）
# - progress（現在の進捗）
# - decisions（採用した決定事項、保留事項）
# - features（実装中・計画中の機能）
# - file_structure（作成・変更したファイル）
# - cautions（注意点・ハマったこと）
#
# =====================================

workflow:
  version: "1.0"
  last_updated: "{{datetime}}"

  last_session_summary: |
    【今回のセッションでやったこと】
    - プロジェクト新規作成
    - prompt/フォルダ作成
    
    【次にやること】
    - 要件定義の開始

  # =====================================
  # 1. 現在の進捗
  # =====================================
  progress:
    current_phase:
      number: 0
      name: "要件定義"
      status: "not_started"
    
    current_task:
      description: "プロジェクトの目的・要件を明確にする"
      started_at: "{{datetime}}"
    
    completed_phases: []
    
    next_tasks:
      - "プロジェクトの目的を確認"
      - "主要機能を洗い出す"
      - "技術スタックを決定"

  # =====================================
  # 2. 決定事項
  # =====================================
  decisions:
    adopted: []
    #  - id: "D001"
    #    date: "{{date}}"
    #    decision: "決定内容"
    #    reason: "理由"
    
    postponed: []
    #  - id: "P001"
    #    idea: "保留にしたアイデア"
    #    trigger: "いつ検討するか"

  # =====================================
  # 3. 実装中・計画中の機能
  # =====================================
  features:
    in_progress: []
    #  - id: "F001"
    #    name: "機能名"
    #    status: "in_progress"
    #    description: "説明"
    
    planned: []
    #  - id: "F002"
    #    name: "機能名"
    #    status: "planned"
    #    description: "説明"
    #    depends_on: ["F001"]

  # =====================================
  # 4. ファイル構成
  # =====================================
  file_structure:
    created:
      - path: "prompt/PROJECT_SPECIFIC.yaml"
        description: "プロジェクト固有設定"
        created_at: "{{datetime}}"
      
      - path: "prompt/WORKFLOW.yaml"
        description: "進捗・決定事項"
        created_at: "{{datetime}}"
      
      - path: "prompt/SYSTEM_PROMPT.yaml"
        description: "AIの振る舞い"
        created_at: "{{datetime}}"
      
      - path: "prompt/ARCHITECTURE.yaml"
        description: "実装済み機能"
        created_at: "{{datetime}}"
    
    to_create: []
    
    to_modify: []

  # =====================================
  # 5. 注意点・学び
  # =====================================
  cautions: []
  #  - id: "C001"
  #    category: "カテゴリ"
  #    content: "注意点の内容"
  #    reason: "なぜ注意が必要か"
