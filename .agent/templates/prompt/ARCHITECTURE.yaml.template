# =====================================
# 🏛️ ARCHITECTURE.yaml テンプレート
# =====================================
#
# 【使い方】
# /start-project 実行時に、このテンプレートがプロジェクトルートの
# prompt/ARCHITECTURE.yaml にコピーされる。
# 機能の実装が完了したら、このファイルに移動する。
#
# 【このファイルに書くこと】
# - プロジェクト概要
# - 実装済み機能一覧
# - アーキテクチャ設計
# - 技術仕様
#
# =====================================

architecture:
  version: "1.0"
  last_updated: "{{date}}"

  # =====================================
  # 1. プロジェクト概要
  # =====================================
  project:
    name: "{{project_name}}"
    goal:
      primary: "{{primary_goal}}"
      target_user: "{{target_user}}"
      success_criteria:
        - "{{criteria_1}}"
        - "{{criteria_2}}"

  # =====================================
  # 2. 実装済み機能一覧
  # =====================================
  completed_features:
    # 機能が完了したらここに追加
    # 
    # phase1:
    #   name: "フェーズ名"
    #   status: "done"
    #   completed_at: "{{date}}"
    #   features:
    #     - id: "F001"
    #       name: "機能名"
    #       description: "説明"
    #       file: "path/to/file"
    #       status: "done"

  # =====================================
  # 3. アーキテクチャ設計
  # =====================================
  architecture_design:
    overview: |
      アーキテクチャの概要をここに記述
    
    layers: []
    #  - layer: "レイヤー名"
    #    content: "内容"
    #    location: "ファイルパス"
    
    file_structure:
      description: |
        ファイル構成の説明

  # =====================================
  # 4. 技術仕様
  # =====================================
  technical_specs:
    # 実装した技術的な仕様をここに記録
    # 
    # api:
    #   endpoints:
    #     - path: "/api/example"
    #       method: "GET"
    #       description: "説明"
    # 
    # database:
    #   tables:
    #     - name: "table_name"
    #       description: "説明"
